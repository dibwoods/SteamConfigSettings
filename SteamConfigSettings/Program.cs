using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SteamConfigHelper {
    class Program {
        static void Main(string[] args) {
            /*
            SteamLocalConfig config = new SteamLocalConfig();
            config.SetAppSetting("507490", "LaunchOptions", "/nolaunch");
            if (config.GetAppSetting("507490", "LaunchOptions", out string value)) {
                Console.WriteLine(value);
            }
            */
        }
    }
}
